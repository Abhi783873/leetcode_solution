#include <string.h>

int minOperations(char **logs, int logsSize) {
    int count = 0;

    for (int i = 0; i < logsSize; i++) {

        if (strcmp(logs[i], "../") == 0) {
            if (count > 0) {
                count--;
            }
        }
        else if (strcmp(logs[i], "./") == 0) {
          
        }

        else {
            count++;
        }
    }

    return count;
}
